# UAT hygiene — stamp + Catalog click-path

**Tip parent:** `07ea593` · **Owner:** Neo · Chrome: davinci

## Fixes
- Header `version-badge` now uses `crate::CRATE_STAMP` (`0.0.36-dev`) — was hardcoded `0.0.35-dev`.
- IDE status `git_branch` / native daemon fallback / webrtc agent string aligned to `CRATE_STAMP`.
- Catalog tab label: **Catalog · Lexicon**; bay title **Catalog · Lexicon packs**.
- Red honesty **missing** chip ≠ lexicon held-gate (gate copy is **held / not yet — open lexicon pack**).

## Click-path
Script/Code IDE → Zone D → **Catalog · Lexicon**, or VibeScript console peer below editor.

## Chrome label
- Vibe console / cell **Run** → **Play** (studio lexicon).
