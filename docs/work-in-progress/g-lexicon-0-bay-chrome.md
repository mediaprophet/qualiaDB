# G-LEXICON-0 — Poet studio-bay chrome (davinci / monet)

**Status:** landed on tip parent `a7e50f3285b008c5a4063314f59ac8e278878ce7` (`0.0.36-dev`)  
**Bind:** `GraphDatabase.lexicon_manifest` (already in `ALL_BOUND`) · **no Host widen** · **no in-binary WordNet** · **no dotted `qualia.*`**

## What landed

Catalog-peer chrome in the Poet studio bay (`lexicon_bay.rs`):

| Surface | Behaviour |
|---------|-----------|
| Held-gate | Path → `GraphDatabase.lexicon_manifest`. Missing / unknown / E300 / no daemon → **held / not yet** + why-text “open lexicon pack”. Never “broken”. |
| Arrive pack card | Success paints `packSemVer` + framing chip set. |
| Chips | **living** (warm, person/living/country) · **artifact** (crisp, tool/volume/file) · **machine** (muted, `Capability.method` secondary). Framing drives the set; **mixed splits**. |
| Upgrade recipe | arrive on pack open · hold on breaking-id list · leave on dismiss · **commit only on a real pack write** (this slice is listen-only). Reduced-motion still arrive/hold/leave. |

Mounts: Zone D Catalog tab (`ide.rs`) and the live Vibe console Catalog peer.

## Tokens

`--chip-living` / `--chip-artifact` / `--chip-machine` in `01-tokens.css`; chip + held-gate + recipe beats in `15-studio-chrome.css`.

## Out of scope

WordNet engine · Host widen · `lexicon:` pin / alias diagnose (Vibe) · Solid · G-COORD
