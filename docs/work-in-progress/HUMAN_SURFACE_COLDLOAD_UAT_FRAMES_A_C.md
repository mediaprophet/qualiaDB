# Cold-load UAT — Frames A–C (Capt gate)

**Branch:** `0.0.38` · **Parity tip:** `c6ab0f6` · **Lexicon tip:** `541c3a6`  
**Surfaces:** Desktop Poet + WASM (trunk / installed) — **same steps, same words**  
**Owners:** Capt (run) · davinci (chrome expect) · monet (motion feel on A)  
**Pass bar:** a person finishes without an agent; no Capability.method as primary label.

---

## Prep

1. Pull `0.0.38` tip ≥ `c6ab0f6`.  
2. Daemon `:4242` if Keep/Ask need live graph — health `/health`.  
3. Open **WASM** first (harder path), then **Desktop**.  
4. Reduced-motion once on each surface (still-arrive / still-hold still named).

---

## Frame A — First arrive

**Expect (lexicon §7.A):** studio bay · Ask · Keep · Play · advanced muted.

| # | Step | Pass if | Fail if |
|---|------|---------|---------|
| A1 | Cold open Poet (no agent script) | Sees studio bay, not a terminal-first wall | Only manifolds/tools/Capability soup |
| A2 | Read primary chrome | Finds **Ask** / **Keep** / **Play** (or Ask graph · Keep volume · Play cell) without hunting invoke ids | Must know `GraphDatabase.*` to start |
| A3 | Soft-rise | First paint feels arrive (soft), not slap | Hard cut / modal stack |
| A4 | Radial wheel | Right-click (or long-press) shows radial wheel | No wheel / browser default only on WASM |
| A5 | Capability.method | Method strings secondary/muted | Primary button labels are dotted methods |

**davinci chrome next (if Fail):** empty-state mount with Frame A copy; mute advanced; prove `radial_menu` fires on WASM human gesture.

---

## Frame B — Held / not yet

**Expect:** held / not yet — never broken.

| # | Step | Pass if | Fail if |
|---|------|---------|---------|
| B1 | Catalog · Lexicon with no pack / daemon down | **held / not yet — open lexicon pack** | broken / stub / red missing as lexicon gate |
| B2 | Living · artifact · machine chips visible | Three chips; machine = Capability secondary | Single “identity” chip |
| B3 | Held look | Steady hold beat — not panic red | Feels crashed |
| B4 | Other “unavailable” panels | Prefer held / not yet when wait-honest | Broken theatre for wait states |

**davinci chrome next:** align Pulse/Job/Mesh copy where product means wait-honest.

---

## Frame C — Living-safe vs tool

**Expect:** who/life warm · tools/handles crisp · no Thing-wash.

| # | Step | Pass if | Fail if |
|---|------|---------|---------|
| C1 | Mixed lexicon arrive (if pack open) | living + artifact split | One identity bag |
| C2 | Spot toolchest / ontology labels | Persons/countries not called things/objects | Thing-wash living subjects |
| C3 | Plane speak | who · claim · handle · tool nowhere collapsed in primary chrome | “Identity” as who-token |

**Marvin cite** if C2/C3 fail; davinci copy sweep.

---

## Report template (Capt → Neo)

```
Tip: <sha>
Surface: WASM | Desktop
Frame A: PASS/FAIL — notes (A1–A5)
Frame B: PASS/FAIL — notes
Frame C: PASS/FAIL — notes
Wheel: PASS/FAIL
Sayables-first: PASS/FAIL
Screenshots: <paths>
```

Wave-22 stays **held** until A+B cold-load PASS on **both** surfaces (C can PARTIAL with Marvin follow-up).

---

## Chrome backlog (davinci) after first FAIL batch

1. Frame A empty-state component (lexicon copy verbatim).  
2. WASM radial wheel human-path UAT + fix (`radial_menu.rs` already in tree — prove gesture).  
3. Mute Capability.method on cold-load primary.  
4. held/not yet sweep for wait-honest panels.  
5. No Host invent — live catalog only.
