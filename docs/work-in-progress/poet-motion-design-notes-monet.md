# WIP — Poet motion / visual design notes (monet)

**Status:** work-in-progress · **Not standards** · **Freeze:** `vibe-host-0.1` @ `6dc2b8b8` · **Sync tip:** INDEX tip-lock / `d2b79211`+ · **Pair:** davinci
**Constraint:** no new coding from this crew while G-COORD advances elsewhere. Design markdown only.

## Motion contract (v0 — named beats only)
- **entrance** = soft rise + light fade (Stage depth cue)
- **dwell** = steady focus + quiet breath on active track
- **exit** = dissolve along the same z-path
- No free tweens. Reduced-motion: same beats, shorter/crossfade variants — state never depends on motion alone.

## UAT accept (look/motion) — fill beside Vibe’s checklist
- **A office:graph:** glyph renders; sparql run = dwell breath; results settle = dwell hold; close = exit; gated daemon looks gated (why-text), not broken.
- **B sanctuary:** open dock = entrance; commit beat **only** on real `volume_commit` success; deny/fault/E300 = gated, never “saved” celebration.
- **Glow:** diagnose span lights cell/token; if coarse, note for Vibe B row — don’t fake precision.
- **Copy:** never call persons or living beings “things” (`shacl-first-vs-owl-ok-class-list.md`). Prefer “person / living / country” vs “tool / volume / file”.

## Visual tokens (design ahead)
1. Dark/light + contrast token sheet so toolchest, volume dock, map skins stay coherent.
2. Touch/dense hit targets for toolbar + map scrubber (pen/touch, not mouse-only).
3. Provenance density: full trail ↔ summary chip (collapsible) so Stage stays readable.
4. Volume `q42:state` looks: closed · open · committed · denied · fault — distinct, human.

## G-COORD — visual ahead of bind (no code)
1. One map chrome shell; **skins** for geo vs cosmos/fiction/speculative/POV.
2. Temporal scrubber always on map Timeline.
3. Quiet spatiotemporal badge on language cells (living vs artifact subject per Marvin list).
4. Progressive disclosure on dense Stage — summary first, detail on demand.
5. QDNF / Solid out of map visual copy.

## Created vs living (visual language)
- Artifact chrome (volume, toolchain, CRS): tool/file language + cooler neutrals.
- Living/person/country content: warmer, non-commodity labels — never “thing” framing in icon metaphors either.
- Mixed Position: living-safe words for *what* is placed; coords as secondary technical detail.

## Aspect coverage checklist (design)
Every shipped surface eventually needs Layout + Stage + Timeline **aspects** + named beats. They are readings, not identical copies. Regressions = missing depth, missing time, or free-tween creep.

## Out of scope now
Implementation · next inventory toolchain · Solid IdP · Host invent · dotted `qualia.*` · fake durable save/preview
