# docs/to-do — outdated docs needing an update

Markdown parked here is **outdated** — it describes work that has drifted from the code and needs either a
rewrite-in-place or deletion once superseded. It is *not* legacy (that work isn't necessarily done) and it is
*not* a plan (it isn't describing unbuilt future work). Triage each file, fix or fold it into the right home,
then remove it from here.

- Fully-implemented work → `legacy-docs/` (gitignored reference).
- Not-yet-implemented / future work → `docs/plans/`.
- Active reference (how the system works today) → `docs/manuals/`.
