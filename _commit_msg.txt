feat: implement compliance rule engine + reliability analyzer + dispatch_payment

Category C cleanup:
- Remove last dead import (CsdManager) from statistical_computing.rs
  (Instant is still used, kept). 598 warnings (down from 601).

Moderate #8 — Compliance rule engine (financial_modeling):
- Implement ComplianceMonitor::check_compliance with real rule evaluation
  for PositionLimit, KYC, AML, MarginRequirement, TradingRestriction, Custom.
- Add ComplianceMonitor::add_rule to register rules.
- Empty portfolio + no rules = Compliant; non-empty + no rules = Flagged
  (never fabricates compliance).
- Add PortfolioAction::ComplianceCheck variant for audit entries.
- 12 new tests covering all rule types, mixed pass/fail, edge cases.
- Update test_compliance_check to expect Flagged (not Err) — the old
  test expected the stub's NotImplemented error.

Moderate #12 — ReliabilityAnalyzer::analyze (engineering_analysis):
- Implement real first-principles reliability analysis from model materials
  and loads: stress = F/A, safety factor = yield/stress, reliability index
  β = (SF-1)/σ_SF, failure probability P(fail) = Φ(−β).
- Add normal_cdf (Abramowitz & Stegun 7.1.26 approximation).
- CoV model: 0.10 baseline + ductility adjustment from ultimate/yield ratio.
- Missing material or loads → InsufficientData (never fabricated).
- 5 new tests: safe model, yield-exceeded, no material, no loads, results_id.
- Update test_reliability_analysis to test both InsufficientData (bare model)
  and real analysis (model with material + load).

Moderate #13 — IlpDispatcher::dispatch_payment (services):
- Add dispatch_payment convenience method wrapping dispatch for single
  MicropaymentInstruction. SwarmVerify::verify already implemented (Freivalds).

455/455 specialized_libs tests pass. 6/6 ilp_dispatcher tests pass.
Workspace compiles clean.
