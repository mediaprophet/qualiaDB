{
  "name": "run_docs_tests",
  "description": "Run docs/tests headless suites (logic, wasm, native, or both). Native/both require daemon on localhost:4242.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "mode": {
        "type": "string",
        "enum": ["logic", "wasm", "native", "both"],
        "description": "Test mode matching docs/tests/run-headless.mjs"
      }
    }
  }
}