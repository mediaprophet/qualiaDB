{
  "name": "query_graph",
  "description": "Run guarded graph traversal against the in-memory daemon graph.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "sanctuary_override": {
        "type": "string",
        "description": "Cryptographic sanctuary override token for egress-gated traversal"
      }
    }
  }
}