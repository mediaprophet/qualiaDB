# Skill: VibeScript (`vibe-0.1`)

Canonical published copy: `docs/vibe/SKILL.md`. Follow that file.

Normative language: `docs/manuals/standards/vibescript-core.md`.
Diagnose: `vibe::diagnose(src)` → JSON with `error_code`, `span`, `suggested_fix`.
Catalog: `capability.invoke("CapabilityDiscovery.catalog", null)`.
