# NL → Vibe pair (parses and type-checks)

**Natural language:** Count graph rows where the condition kind matches, at most 64.

**Source:** `../12_3_count.vibe`

`graph.query` **must** include `take: 64`.
