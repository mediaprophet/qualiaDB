module <https://qualiadb.org/modules/emf_to_color>;
import "vibe:0.1/math" as m;
pure fn wavelength_to_rgb_channel(wavelength: f64, min_nm: f64, max_nm: f64) -> f64 {
    return m.clamp(wavelength, min_nm, max_nm);
}
