module <https://qualiadb.org/modules/svg_path>;
import "vibe:0.1/math" as m;
pure fn point_distance(x1: f64, y1: f64, x2: f64, y2: f64) -> f64 {
    let dx = x2 - x1;
    let dy = y2 - y1;
    return m.max(0.0, dx * dx + dy * dy);
}
