module <https://qualiadb.org/modules/reactive_compose>;
requires [ capability("graph.read") ];

cell seed := 0.0;

pure fn sum_quins(rows: List<f64>) -> f64 {
    let mut total: f64 = 0.0;
    for v in rows {
        total = total + v;
    }
    return total;
}
