module <https://qualiadb.org/modules/reactive_color>;
import "vibe:0.1/math" as m;

present hue {
    color: #ff2d95
}

cell hot := 360.0;
cell cold := 0.0;

pure fn temperature_to_hue(temp: f64) -> f64 {
    return m.clamp(temp, 0.0, 360.0);
}
