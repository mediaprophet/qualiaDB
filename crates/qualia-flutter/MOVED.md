# Moved

The Flutter desktop app lives at:

`Local_LIbraries/EXPORTS/qualia-flutter-desktop/crates/qualia-flutter`

See `Local_LIbraries/EXPORTS/README.md` for WebizenAI repo setup.

Delete this stub directory once no process holds `.dart_tool` (close qualia_flutter.exe / IDE analysis).
