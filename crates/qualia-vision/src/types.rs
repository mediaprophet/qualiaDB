//! Stable visual ABI — re-exported from specialized_libs::computer_vision (MIG-V2).

pub use qualia_core_db::specialized_libs::computer_vision::types::{
    Detection, ImageView, PixelFormat, VisionError, VisualCapabilities, VisualModel,
    VisualOutputCounts, MAX_DETECTIONS, MAX_EMBED_DIM,
};
