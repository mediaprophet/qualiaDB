# Visual fixtures (redistribution-safe)

Synthetic RGB byte patterns used by unit tests — **not** a production eval set.

| File | Description |
|------|-------------|
| `solid_red_8x8.rgb` | 8×8 RGB, all (255,0,0) |
| `solid_blue_8x8.rgb` | 8×8 RGB, all (0,0,255) |
| `expected_red.json` | Expected class hash for CPU reference on red |

**H1 (Timothy):** lawful real-image eval corpus still required before claiming production classifier quality.
