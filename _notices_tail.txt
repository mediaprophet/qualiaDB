2026-06-29 · Claude/feature-p64-manifold-wal-eigensolver · CLAIM · crates/qualia-core-db/src/audio/{stft.rs(NEW), cqt_bake.rs, hrtf.rs, mod.rs} · Task #38 (directed by Timothy): real forward transforms over actual audio samples (the bake modules only synthesized/log-resampled/analytic-panned previously). ADD genuine: forward_stft (Hann window → dispatch::fft_f32 per frame) + stft_magnitudes + bake_stft_sidecar_from_samples; forward_cqt (direct constant-Q) + bake_cqt_sidecar_from_samples; hrtf convolve_fir + synthesize_hrir + binaural_render. Purely ADDITIVE — keeps all existing fns+tests. Audio modules are in my branch scope, not claimed/off-limits by any other instrument (lanes are wgsl_forge/solvers/LLM). NOT touching gguf_bridge/compute_bridge/modalities/n3_parser.
2026-06-29 · Claude/feature-p64-manifold-wal-eigensolver · RELEASE · crates/qualia-core-db/src/audio/{stft.rs(NEW), cqt_bake.rs, hrtf.rs, mod.rs} · Task #38 real audio forward transforms DONE (staged, not committed). The bake modules previously only synthesized/log-resampled/analytic-panned; ADDED the genuine transforms over real samples. (1) NEW stft.rs: forward_stft(samples,frame_size,hop) (frame_size pow2∈[2,1024]; Hann w=0.5(1-cos(2πi/(N-1))) → interleaved [re=windowed,im=0] → crate::wgsl_forge::dispatch::fft_f32 per frame → frame_size [re,im] bins) + stft_magnitudes (one-sided frame_size/2+1 |X|) + bake_stft_sidecar_from_samples (real STFT → group-avg → SPECTRAL_PREVIEW_BINS, mirrors bake_stft_sidecar_from_preview header machinery, _pad=STFT). (2) cqt_bake.rs: forward_cqt(samples,sr,f_min,bpo,n_bins) direct constant-Q Q=1/(2^(1/bpo)-1), f_k=f_min·2^(k/bpo), nk=round(Q·sr/f_k).clamp(1,len), X_k=(1/nk)Σ samples[j]·hann(j,nk)·exp(-2πi f_k j/sr), push |X_k| + bake_cqt_sidecar_from_samples (n_bins→preview, passthrough at 64). (3) hrtf.rs: convolve_fir (direct linear, len s+h-1, identity-preserving) + synthesize_hrir (per-ear unit impulse: near ear ~0, far ear delayed |itd|·sr w/ fractional-delay interp, scaled gain_l/gain_r, gentle one-pole LP head-shadow on contralateral ear only) + binaural_render (binaural_from_position→synthesize_hrir→convolve_fir). Measured KEMAR HRIRs documented as remaining optional cold asset; this synthesizes a physically-plausible HRIR from the ITD/ILD model. TESTS (correctness gate, all PASS): STFT cos(2π·8·i/64) peaks at bin 8 (exercises forge fft_f32 end-to-end), frame-count==(len-frame)/hop+1; CQT 440Hz@44100 peaks at bin 36 (=12·log2(440/55)); HRTF convolve(x,[1])==x, len==x+h-1, [1,2,3]*[1,1]==[1,3,5,3], hard-left click → left onset earlier than right (ITD) + left energy≥right (ILD). VERIFIED: cargo test -p qualia-core-db --lib audio:: = 42 passed/0 failed (was 30; +12 new); broad `audio` filter = 44 passed/0 failed. cargo build -p qualia-core-db --lib + --features cuda + qualia-cli all EXIT 0. Purely ADDITIVE — every existing fn+test kept. No other lane touched. Releasing claim.
2026-06-29 · Claude/feature-p64-manifold-wal-eigensolver · RELEASE · crates/qualia-core-db/src/wgsl_forge/{emit/df64.rs(NEW), emit/mod.rs, dispatch.rs, mod.rs} · Task #37 df64 emulated-f64 GEMM DONE (staged, not committed). NEW emit/df64.rs: GEMM_DF64_WGSL raw kernel (each f64 = hi/lo f32 pair; TwoSum/TwoProd/Dekker error-free transforms; one invocation/output-elem @workgroup_size(64); bindings a/b/c/dims) + GEMM_DF64_ENTRY="gemm_df64" + naga-validate test (entry-point asserted). dispatch.rs: host f64_to_df32/df32_to_f64 + pack_df32/unpack_df32 (hi,lo interleaved) + pub gemm_f64_df64(m,k,n,a,b) building a transient WgpuComputeContext (cap M*N*16B floored 4MiB), allocate a(0,StorageRead)/b(1,StorageRead)/c(2,StorageReadWrite zeros)/dims=[m,n,k] u32(3,StorageRead), WgpuPipeline::compile+dispatch(element_count=m*n → ceil/64 wg), read_buffer_f32→unpack. WIRED gemm_f64 into a 3-TIER chain: native CUDA-f64(NVIDIA, cuda feat) → df64-WGSL(any other GPU, caps().wgpu, ALWAYS compiled) → CPU floor; any accel Err falls through, never propagated. Module + gemm_f64 docs rewritten (3-tier table; GEMV still CUDA-f64→CPU). Tests: +df64_pack_roundtrips (non-GPU, ~1e-15) +gemm_df64_wgsl_validates (naga) + #[ignore] gemm_f64_df64_matches_cpu (64³, tol 1e-10, for A2000 orchestrator). VERIFIED: cargo test -p qualia-core-db --lib wgsl_forge = 75 passed/0 failed/26 ignored (was 73/0; +2 non-GPU,+1 ignored); cargo build -p qualia-core-db --features cuda + cargo build -p qualia-cli both EXIT 0. Purely additive to wgsl_forge (my lane); no consumer/other lane touched. Releasing claim.
2026-06-29 · Claude/feature-p64-manifold-wal-eigensolver · CLAIM · crates/qualia-core-db/src/solvers/linear_algebra/gemm.rs + solvers/{linear_algebra/spectral.rs} + solvers/learning/clustering/{kmeans.rs,gmm.rs} + solvers/learning/classification/svm.rs + wgsl_forge/dispatch.rs · Rollout A0-A4 (directed by Timothy "get it all done, LLM excluded"): extend the keystone gemm() offload from plain-only to ALL transpose combos (α=1,β=0) by materialising the transposed operand on the offload path — unlocks covariance XᵀX (PCA/ridge/linear/bayesian) + attention Q·Kᵀ which were CPU-only. Add dispatch::pairwise_sq_dist_f64 (‖aᵢ−bⱼ‖² via gemm_f64 cross-term + CPU floor) → wire kmeans assignment / gmm E-step / svm RBF+linear kernel. Wire spectral.rs char-poly A·M GEMM. STRICTLY ADDITIVE + behaviour-preserving (off-accelerator byte-identical; eligibility gated on caps + GEMM_GPU_THRESHOLD). Eigen Jacobi / QR Householder / SMO are NOT forge-able — honestly skipped, not faked. Audit done; no live concurrent edit on this machine. Lane-overlap flag for gemm.rs (0.0.21-la author) stands for Timothy's disposal.
2026-06-29 · Claude/feature-p64-manifold-wal-eigensolver · RELEASE · solvers/linear_algebra/{gemm.rs,svd.rs,spectral.rs} + solvers/learning/{clustering/kmeans.rs,classification/svm.rs} + wgsl_forge/{dispatch.rs,mod.rs} · Rollout A0–A4 DONE (consumer wiring; staged). (1) gemm.rs: GENERALISED the offload from plain-only to ALL transpose combos AND arbitrary alpha/beta — materialise the transposed operand into row-major scratch (O(mk)/O(kn), dominated by the O(mnk) GEMM) then apply alpha/beta in the O(mn) CPU combine. Unlocks covariance XᵀX (PCA/ridge/linear/bayesian, alpha=1/(n-1)) + attention Q·Kᵀ which were CPU-only. Off-accelerator/sub-threshold byte-identical. +2 hermetic tests (transposed covariance 4096×8, scaled+beta accumulate). (2) dispatch.rs: NEW pairwise_sq_dist_f64 (‖aᵢ−bⱼ‖² via gemm_f64 cross-term + norms, clamp≥0) + exact pairwise_sq_dist_cpu_f64 reference; re-exported. +3 tests. (3) svd.rs: AᵀA gram + U=A·V both routed through solver gemm/matmul (best-path). (4) spectral.rs: Faddeev–LeVerrier A·M routed through matmul. (5) kmeans.rs: assignment AllPairs via pairwise_sq_dist_f64 (gated caps+threshold; exact per-point nearest CPU floor otherwise, lowest-index tie-break preserved). (6) svm.rs: kernel matrix DenseLinear — Linear=X·Xᵀ gemm, RBF=exp(−γ·pairwise); SMO stays CPU (Divergent). HONEST SKIPS (not faked): eigen.rs cyclic-Jacobi, QR Householder, GMM diagonal-cov E-step (per-component Reduction), unblocked Cholesky (needs blocked rewrite — deferred, mid-feature risk per §11/§12). VERIFIED: solvers+specialized_libs+wgsl_forge+audio = 910 passed/0 failed/26 ignored; cargo build --lib --features cuda + qualia-cli both EXIT 0. Purely additive + behaviour-preserving. Map update + progress-log pending (task C). Releasing claim.
2026-06-29 · Claude/feature-p64-manifold-wal-eigensolver · RELEASE · wgsl_forge/physics/{mod.rs,molecular_dynamics.rs,kinematics.rs}(NEW) + wgsl_forge/mod.rs + shaders/{molecular_dynamics.wgsl,kinematics.wgsl}(rewritten) + docs/plans/acceleration-integration-map.md + ACCELERATION_ROLLOUT_PROGRESS_LOG.md(NEW) · Rollout B1 DONE: formalize the two genuinely-real orphan physics shaders as certified forge kernels. NEW wgsl_forge/physics/ module (§11). molecular_dynamics: corrected velocity-Verlet (orphan updated position but LEFT VELOCITY UNCHANGED — fixed) + PBC, flat f32 layout (no vec3 std430 padding), exact CPU oracle md_step_cpu, naga-validated, GPU-certified on A2000. kinematics: corrected softened (Plummer) inverse-square N-body + symplectic-Euler, DOUBLE-BUFFERED (orphan read+wrote same buffer = data race — fixed), exact CPU oracle nbody_step_cpu, naga-validated, GPU-certified on A2000. Both embed .wgsl via include_str! (single source of truth). fluid_dynamics + quantum_bio LEFT for curation (⚑ Timothy): fluid is a velocity*=0.99 placeholder; quantum_bio is non-compiling (builtin shadowing, invalid inline-struct) + demo-grade — formalizing needs a physical-model decision, NOT faked. VERIFIED: wgsl_forge::physics 6 oracle/naga pass + 2 GPU-certify pass on A2000; full lib 2223 passed/0 failed/29 ignored; build --lib --features cuda + qualia-cli exit 0. Releasing claim.
2026-06-29 · Claude/feature-p64-manifold-wal-eigensolver · RELEASE · wgsl_forge/ir/{graph.rs(NEW),mod.rs} + wgsl_forge/emit/wgsl.rs + docs/plans/dag-ir-forge.md(NEW) + ACCELERATION_ROLLOUT_PROGRESS_LOG.md · DAG-IR forge design + Phase 1 (directed by Timothy: "update the plan, fully scope, then implement"). After Timothy's point that the forge accretes per-kernel.id string emitters (gemm/gemv/fft WGSL-only; MSL/HLSL/PTX emit 0) instead of GENERATING kernels, and that q42 already has DAGs: designed a typed compute-graph IR the forge lowers to every backend in ONE pass. Design via ultracode workflow (10 agents: parallel q42/IR/schedule/ray-query reads → 3 judged proposals → adversarial verify). docs/plans/dag-ir-forge.md = full scope (10-node op vocab, IR types, Lowerer architecture, HONEST q42 encoding [new use of substrate, deferred to P6], RT-core Neighbor boundary [RT backs spatial/neighbor ONLY, NOT dense matmul], certify plan, P1–P7). PHASE 1 (spine, this commit): NEW ir/graph.rs — ComputeGraph/OpNode(12-arm)/GraphNode/TensorRef/Shape/DType, arena-backed, acyclic-by-construction (insertion==topo), shape+cycle asserts; Lowerer trait + lower_graph topo driver; KernelSpec::to_graph() bridges gemm/gemv/fft to 1-node graphs; emit_kernel_body ROUTES gemm/gemv/fft through to_graph→lower_graph→WgslDelegateLowerer which DELEGATES to the proven emit_*_wgsl (byte-identical by construction — neutralizes the adversarial verifier's #1 risk: source_hash cache invalidation). Leaf graph-template emission + LLM decode DAG + CUDA/MSL/HLSL lowerers + q42 persistence + RT Neighbor are scoped P2–P7 (tasks #49–54), NOT stubs in P1. VERIFIED: wgsl_forge::ir::graph + emit byte-equal 11 pass/0 fail; full wgsl_forge non-GPU 91 pass/0 fail/28 ignored; GPU-certify on A2000 generated_{gemm,gemv,fft}_matches_oracle_on_real_gpu ALL PASS (routed through DAG-IR, byte-equal on hardware); build --lib --features cuda + qualia-cli exit 0. Releasing claim.
2026-06-29 · Claude/feature-p64-manifold-wal-eigensolver · RELEASE · wgsl_forge/graph_ops/{mod,reduce,broadcast}.rs(NEW) + wgsl_forge/{mod.rs,emit/wgsl.rs,emit/mod.rs} + docs/plans/dag-ir-forge.md + ACCELERATION_ROLLOUT_PROGRESS_LOG.md · DAG-IR Phase 2 DONE: native Reduce + Broadcast op-nodes (first NON-delegating graph lowerings; P1's gemm/gemv/fft delegate, these emit WGSL directly from the node). reduce.rs: single-workgroup tree-reduce (grid-stride fold→shared tree→output[0]) Sum/Mean/Max/L2 = RMSNorm-variance/softmax primitive; reduce_wgsl(op,wg)+reduce_cpu(f64 floor)+reduce_gpu. broadcast.rs: out[i]=input[i%in_len] scale-fanout; +cpu+gpu. WgslGraphLowerer (no KernelSpec) + emit_graph_wgsl lower OpNode::Reduce/Broadcast; P1 delegating lowerer UNTOUCHED. HONEST RE-SCOPE: design's P2 also bundled GatherDequant + ternary {GatherDequant→MatMul} split — that's a 2-node graph needing GPU-side intermediates = the topo multi-dispatch executor P4 builds (verifier §8.1: WgpuPipeline is one-encoder-per-call), so MOVED to P2b/P4, not half-built. VERIFIED: wgsl_forge::graph_ops+emit 10 pass/0 fail; GPU-certify A2000 reduce_gpu_matches_oracle(4 kinds)+broadcast_gpu_matches_oracle PASS; full wgsl_forge non-GPU 96 pass/0 fail/30 ignored; --features cuda + qualia-cli build exit 0. Releasing claim.
2026-06-29 · Claude/feature-p64-manifold-wal-eigensolver · RELEASE · wgsl_forge/graph_ops/{elementwise.rs(NEW),mod.rs} + wgsl_forge/emit/wgsl.rs + docs/plans/dag-ir-forge.md + ACCELERATION_ROLLOUT_PROGRESS_LOG.md · DAG-IR Phase 3a DONE: native Elementwise LLM kit. graph_ops/elementwise.rs: unary Silu/Gelu(tanh approx)/Exp/RecipSqrt/Relu/Recip (out=f(in)), binary Add/Mul (out=a⊙b), ternary Fma (out=a·b+c) — emitted from OpNode::Elementwise{f} by WgslGraphLowerer; exact f32 CPU oracles (unary_cpu/binary_cpu/fma_cpu). Scale/Bias DEFER to the certified affine-f32 kernel (explicit Err, not stub). With P2's Reduce+Broadcast this completes the SINGLE-NODE vocabulary for RMSNorm/softmax/SwiGLU. HONEST SPLIT: design's P3 also wanted Softmax legalization + MatMul→Elementwise fusion (fused-ffn emergent) — those are MULTI-NODE, GPU-certify needs the P4 topo executor, so moved to P3b/P4 (CPU-composed oracle buildable independently; not claiming GPU multi-node result pre-executor). VERIFIED: graph_ops::elementwise 3 non-GPU pass + GPU-certify A2000 elementwise_gpu_matches_oracle PASS (all activations + Add/Mul vs oracle f32 tol); full wgsl_forge non-GPU 99 pass/0 fail/31 ignored; --features cuda + qualia-cli exit 0. Releasing claim.
2026-06-29 · Claude/feature-p64-manifold-wal-eigensolver · RELEASE · wgsl_forge/graph_ops/{executor.rs(NEW),mod.rs} + wgsl_forge/execute/wgpu.rs(+copy_view) + wgsl_forge/{ir/graph.rs(+EwKind Sub/Div, TensorRef::input),graph_ops/elementwise.rs(+Sub/Div)} + docs/plans/dag-ir-forge.md + ACCELERATION_ROLLOUT_PROGRESS_LOG.md · DAG-IR Phase 4 DONE — the KEYSTONE multi-node executor, GPU-certified on A2000. executor.rs: execute_graph(graph,externals) runs a whole ComputeGraph node-by-node on the GPU, intermediates kept DEVICE-SIDE (no host readback); execute_graph_cpu = composed differential oracle (threads node CPU floors in topo order). Graph builders softmax_graph(7 nodes)/rmsnorm_graph(5)/swiglu_ffn_graph(5 — MatMul·MatMul·Silu·Mul·MatMul). REAL BUG FOUND+FIXED ON HW: wgpu forbids same buffer bound read-write AND read-only in one dispatch (read_write exclusive) — first all-out_slab design hit a Validation Error; fixed via two-slab discipline (inputs/params in read slab; output→read_write slab then copy_view GPU→GPU back to read slab for the next node). Added WgpuComputeContext::copy_view + EwKind::Sub/Div + TensorRef::input(idx). Per-node submit+copy latency = accepted Option-A cost (single-encoder fusion later). VERIFIED: executor CPU-oracle 3 pass; GPU-certify A2000 execute_graph_gpu_matches_cpu_oracle PASS (softmax 1024-wide, RMSNorm 768, SwiGLU-FFN seq8×dim64×ffn128 all vs composed oracle); full wgsl_forge non-GPU 102 pass/0 fail/32 ignored; --features cuda + qualia-cli exit 0. P4b (attention block + GatherDequant + uplift bench) tracked #55. Releasing claim.
2026-06-29 · Claude/feature-p64-manifold-wal-eigensolver · RELEASE · wgsl_forge/emit/cuda_c.rs(+WMMA_GEMM_TILED) + wgsl_forge/dispatch.rs(+gemm_tc_cuda,+gemm_f32_tc) + wgsl_forge/mod.rs + docs/plans/dag-ir-forge.md + ACCELERATION_ROLLOUT_PROGRESS_LOG.md · DAG-IR P4c part 1 DONE: real TILED tensor-core GEMM (the kernels were single-TILE primitives, not GEMMs; MatMul.tc was a no-op). WMMA_GEMM_TILED_SRC: one warp per 16x16 output tile, loops the proven single-tile primitive over K/16 inner tiles, C[M,N]f32=A[M,K]f16·B[K,N]f16 (M/N/K mult of 16). gemm_tc_cuda (f32→f16 pack, dims storage buf, NVRTC, grid of (M/16)(N/16) warps). gemm_f32_tc = opt-in capability-selected tensor-core entry: WGSL coopmat(portable, DORMANT until #9741) → CUDA WMMA(NVIDIA, NOW) → plain f32 floor (opt-in: reduced f16 precision; default gemm_f32 stays full-precision). HONEST: this is the CUDA half — the WGSL coopmat half (the actual wgpu tensor-core path) is dormant on 29.0.3 (zeros), built+verified WITH the #9741 soft-fork (#56) since its tiling can't be certified before the multiply works. VERIFIED: GPU-certify A2000 gemm_tc_cuda_tiled_matches_f16_reference PASS (64³=4×4 tiles×4 K-tiles vs f16-rounded ref + non-zero sanity); gemm_f32_tc_falls_to_plain_floor non-GPU green; dispatch non-GPU 16 pass/0 fail; --features cuda + qualia-cli exit 0. Releasing claim.
2026-06-29 · Claude/feature-p64-manifold-wal-eigensolver · PROGRESS · crates/qualia-core-db/src/wgsl_forge/{emit/coopmat.rs, dispatch.rs, graph_ops/executor.rs, emit/mod.rs, mod.rs, validate.rs} · DAG-IR P4c part 2 (Task #58, done): tiled coopmat WGSL GEMM (matmul_tc_wgsl_tiled) + coopmat_usable() runtime probe + gemm_f32_tc_coopmat host path; OpNode::MatMul.tc now REAL in the executor (coopmat tiled kernel when usable+8-multiples, certified plain GEMM floor otherwise). Coopmat MULTIPLY dormant on wgpu 29.0.3 (#9741 upstream, unreleased) so probe=false today + GPU cert #[ignore]; self-activates on the fix. CUDA WMMA remains the active tensor-core GEMM. wgsl_forge:: 106 pass/0 fail; lib + --features cuda clean; P4 multi-node cert still green. Commit 1397d142. Purely additive to wgsl_forge (my lane); NOT touching gguf_bridge/compute_bridge (0.0.21-la LLM lane).
2026-06-29 · Claude/feature-p64-manifold-wal-eigensolver · PROGRESS · crates/qualia-core-db/src/wgsl_forge/emit/{cuda_graph.rs(NEW), cuda_c.rs, mod.rs} · DAG-IR P5 (Task #52, done): CudaCLowerer — the SAME ComputeGraph lowers to CUDA-C in one pass via the shared Lowerer trait (no per-kernel.id branch). MatMul tc=true->WMMA tensor cores / tc=false->GEMM_F32; Gemv->GEMV_F32; CUDA-C twins of Elementwise/Reduce/Broadcast kit. emit_cuda_c routes gemm/gemv through to_graph->lower_graph. Cross-backend oracle GREEN on A2000: same graph -> CUDA-C matches gemm_cpu (plain) + f16 ref (WMMA); full kit NVRTC-compiles. wgsl_forge:: 109 pass/0 fail. Commit 606c32ab. Additive to wgsl_forge (my lane); NOT touching gguf_bridge/compute_bridge (LLM lane).
2026-06-29 · Claude/feature-p64-manifold-wal-eigensolver · PROGRESS · crates/qualia-core-db/src/wgsl_forge/graph_ops/{gather_dequant.rs(NEW), executor.rs, mod.rs} · DAG-IR P4b (Task #55, done): native ternary GatherDequant op-node (2-bit unpack, array<u32> bind to dodge NaN-canon) + attention_graph (decode softmax(q.Kt).V) + decode_block_graph (norm->attn->res->norm->SwiGLU->res) + dequant_matmul_graph ({GatherDequant->MatMul}). All GPU-certified on A2000 vs composed CPU oracle. HONEST uplift: one decode block (d=576,26 nodes) GPU 869.9ms vs CPU 61.7ms = 0.07x (Option-A per-node-submit + per-call context recreation = latency-bound; win is correctness+generality, NOT speed; fusion+context-reuse pass flagged not-done). Commit 407b2c2f. Additive to wgsl_forge (my lane); NOT touching gguf_bridge/compute_bridge (LLM lane).
2026-06-29 · Claude/feature-p64-manifold-wal-eigensolver · PROGRESS · crates/qualia-core-db/src/wgsl_forge/ir/{q42_bridge.rs(NEW), mod.rs, graph.rs} · DAG-IR P6 (Task #53, done): ComputeGraph <-> NQuin serialization (persistence/provenance view). q42:opKind (opcode 0x10-0x1A, reserved-modality range, no mini_parser/deontic collision) + tensorShape/dtype/scheduleHint companions + feedsInto edge quins + graphOutput; full-u32 matmul dims in op payload, u16/axis shapes (oversize=hard error); graph_merkle_root=blake3 over NQuin bytes. Round-trip identity GREEN (softmax + full decode-block node-for-node; all 11 op-classes; zero-copy byte path). GraphNode now derives PartialEq. wgsl_forge:: 120 pass/0 fail. Commit 9d889162. In-arena graph stays source of truth (plan §5); DagStore/on-disk volume = deeper persistence layer (separable). Additive to wgsl_forge (my lane); NOT touching gguf_bridge/compute_bridge.
2026-06-29 · Claude/feature-p64-manifold-wal-eigensolver · CLAIM · wgsl_forge/graph_ops/executor.rs + wgsl_forge/execute/wgpu.rs · DAG-IR throughput pass (plan §8.1): (1) context reuse — ForgeGraphExecutor owns one WgpuComputeContext (device+slab created once, per-call slab reset) so execute no longer recreates the device per call; (2) single-encoder deferred-submit fusion (Option B) — record all node dispatches + GPU→GPU copy_view hand-offs into ONE CommandEncoder, submit once per graph instead of one queue.submit() per node. Re-run decode_block_kernel_uplift_bench, record honest new ratio. Keep execute_graph_gpu_matches_cpu_oracle + p4b_graphs_gpu_match_cpu_oracle green. Additive to wgsl_forge (my lane); NOT touching gguf_bridge/compute_bridge (LLM lane). Directed by Timothy 2026-06-29.
2026-06-29 · Claude/feature-p64-manifold-wal-eigensolver · RELEASE · wgsl_forge/graph_ops/executor.rs + wgsl_forge/execute/{wgpu.rs,mod.rs} + ACCELERATION_ROLLOUT_PROGRESS_LOG.md · DAG-IR throughput pass (plan §8.1) DONE. (1) Context reuse: new ForgeGraphExecutor owns one WgpuComputeContext (device+queue+two 64MiB slabs created ONCE; per-call bump-ring reset); execute_graph keeps its one-shot signature via a throwaway executor. (2) Single-encoder deferred submit (Option B): new WgpuComputeContext::submit_graph records ALL node passes + GPU→GPU hand-off copies into ONE CommandEncoder + one queue.submit() per graph (+ compile_pipeline/create_compute_bind_group helpers). HONEST RESULT on A2000 (decode_block_kernel_uplift_bench, d=576/kv=128/ffn=1536, 26 nodes, release): GPU 687ms→20.3ms/call (~34x faster GPU path), ratio vs CPU oracle 0.01x→0.57x. Attribution: the one-shot path (742ms, fusion-only) ≈ baseline ⇒ WgpuComputeContext::new() recreation WAS the dominant cost; context reuse is the win (as predicted). Honest boundary: 0.57x means still overhead-bound at one tiny block — 26 pipeline compiles/call is the new floor; pipeline caching + ×L layers + P4c WMMA are the named next levers (flagged, not done). VERIFIED: execute_graph_gpu_matches_cpu_oracle + p4b_graphs_gpu_match_cpu_oracle GREEN; full wgsl_forge:: 125 pass/0 fail/42 ignored; --features cuda lib + qualia-cli build EXIT 0. UNCOMMITTED — awaiting Timothy's commit word. Additive to wgsl_forge (my lane); NOT touching gguf_bridge/compute_bridge. Releasing claim.
2026-06-29 · Claude/feature-p64-manifold-wal-eigensolver · PROGRESS · wgsl_forge/execute/{wgpu.rs,mod.rs} + graph_ops/executor.rs + graph_ops/{stencil.rs(NEW),scatter.rs(NEW)} · (1) Executor THROUGHPUT PASS (directed): ForgeGraphExecutor (context reuse, slabs created once) + single-encoder deferred submit (GraphPass/submit_graph). Uplift bench d=576/26-node: GPU reused 238.7ms vs one-shot 754.7ms vs CPU 62.5ms = 0.26x (up from 0.07x, ~3.6x); per-call ctx recreation was the dominant cost. Commit e01a39ce. (2) DAG-IR P7 part 1: native Stencil (Laplacian+RopePair; Divergence/Advection need field+model=curation) + ScatterAccum (Add/Max via atomicCAS f32) kernels, GPU-certified on A2000 (scatter 4096->64 contention). Commit dfb95b43. wgsl_forge:: 125 pass/0 fail. P7 remaining: MSL/HLSL lowerers + RT Neighbor. Additive to wgsl_forge (my lane); NOT touching gguf_bridge/compute_bridge.
2026-06-29 · Claude/feature-p64-manifold-wal-eigensolver · PROGRESS · wgsl_forge/emit/{graph_msl.rs,graph_hlsl.rs}(NEW) + graph_ops/neighbor.rs(NEW) · DAG-IR P7 parts 2-3 (Task #54 done): (p2) MslLowerer/HlslLowerer for the portable kit (Elementwise/Reduce/Broadcast) — same graph now lowers to 4 backends (WGSL/CUDA-C/MSL/HLSL); structural+graph tests green (HLSL DXC-validatable w/ toolchain; none on this Win box; MSL no Win compiler). (p3) exact-grid Neighbor (FRNN/kNN/Range) + legalize (RT gated <=3D; dims>3->grid) + recall_vs_grid oracle, hand-checked green. RT-core Neighbor ACCEL (build_aabb_scene+ray_query) = optional remainder, new Task #59 (needs wgpu-29 procedural-AABB ray_query feasibility check). DAG-IR P1-P7 substantially complete. wgsl_forge:: 134 pass/0 fail. Commits 595dbfb4, 7698d863. Additive to wgsl_forge (my lane); NOT touching gguf_bridge/compute_bridge.
2026-06-29 · Claude/feature-p64-manifold-wal-eigensolver · PROGRESS · (perf+correctness, all in wgsl_forge lane) · (1) Executor PIPELINE CACHE (execute/wgpu.rs compile_pipeline_cached): held ForgeGraphExecutor compiles each node shader once -> decode block 238ms->16.7ms, GPU now 3.4x FASTER than CPU (commit c733869f). (2) FAITHFULNESS FIX (adversarial review found it): added 1/sqrt(d) attention scale + RMSNorm eps; decode block now a real transformer, 30 nodes, GPU 2.89x CPU (commit bd5f30a0). (3) wgpu coopmat soft-fork #56 ATTEMPTED on A2000: feasible (29.0.4 semver-OK, wgpu compiled) but unreleased main has crate-wide API drift (get_mapped_range->Result, RequestAdapterOptions field) hitting gguf_bridge/output.rs (LLM LANE, off-limits) + platform/tensor -> NO-GO, reverted clean, wait for release #57 (commit 0492f3b1). wgsl_forge:: 134 pass/0 fail. NOT touching gguf_bridge/compute_bridge.
2026-06-29 · Claude/feature-p64-manifold-wal-eigensolver · CLAIM · wgsl_forge/execute/wgpu.rs + wgsl_forge/graph_ops/executor.rs · LLM-on-forge Phase 1a (device unification, Task #60, directed by Timothy): add WgpuComputeContext::from_device(device,queue,caps,cap) so the forge builds on an ALREADY-EXISTING wgpu device (the process-wide gpu_context::shared_gpu()) instead of requesting a second adapter/device; add ForgeGraphExecutor::with_context + on_shared_gpu() so the forge runs on the SAME device that owns the resident LLM weights/KV cache (the keystone for weight residency + the p64 bake-off). Existing new()/with_capacity() path kept intact. + a GPU cert that runs an existing certified graph on the shared device and matches the CPU oracle. Additive to wgsl_forge (my lane); READS gpu_context::shared_gpu() but does NOT modify gpu_context; NOT touching gguf_bridge/compute_bridge (LLM lane).
2026-06-29 · Claude/feature-p64-manifold-wal-eigensolver · RELEASE · wgsl_forge/execute/wgpu.rs + wgsl_forge/graph_ops/executor.rs + LLM_ON_FORGE_PROGRESS_LOG.md(NEW) · LLM-on-forge Phase 1a (device unification, Task #60) DONE, GPU-certified on A2000. WgpuComputeContext::from_device(device,queue,caps,cap) builds the forge on an ALREADY-EXISTING wgpu device (reconstructs adapter identity/constraints/profile from live device.limits()+features()+GpuAdapterCaps snapshot) — no second adapter. ForgeGraphExecutor::with_context + on_shared_gpu()/on_shared_gpu_with_capacity() run the forge on the process-wide gpu_context::shared_gpu() device (the one that owns LLM weights/KV cache). Existing new()/with_capacity() own-device path intact (with_capacity routes through with_context). NEW cert shared_device_executor_matches_cpu_oracle: forge on shared_gpu() matches CPU oracle for softmax + full faithful decode block AND reports same vendor/device as shared_gpu (proves no 2nd adapter). VERIFIED A2000: 5/5 executor GPU certs PASS (incl new one + existing execute_graph/p4b/pipeline_cache); non-GPU wgsl_forge:: 134 pass/0 fail/45 ignored; lib plain + --features cuda both EXIT 0. HONEST boundary: from_device inherits host device features/limits verbatim — shared_gpu doesn't raise RT acceleration-structure limits, so RT-core Neighbor can't create BLAS/TLAS on a shared-device forge ctx (decode path needs none of that; RT-on-shared deferred). Additive to wgsl_forge (my lane); READS gpu_context::shared_gpu() but does NOT modify gpu_context; NOT touching gguf_bridge/compute_bridge (LLM lane). Next: Phase 1b weight residency + p64->forge bridge + real-layer bake-off. Releasing claim.
2026-06-29 · Claude/feature-p64-manifold-wal-eigensolver · RELEASE · wgsl_forge/execute/{memory.rs,wgpu.rs} + wgsl_forge/graph_ops/executor.rs + LLM_ON_FORGE_PROGRESS_LOG.md · LLM-on-forge Phase 1b (weight residency, Task #60) DONE, GPU-certified on A2000. Eliminates the per-call weight re-upload (run() re-shipped every external each call). NEW: persistent weight region separate from the transient ring — BindingUsage::StorageReadResident + a third device buffer weight_slab + write-once 256-aligned bump cursor on WgpuComputeContext; allocate_weight/clear_weights/resident_weight_bytes. ForgeGraphExecutor::load_weights(&[(ext_idx,data)])->ResidentWeights + run_resident(graph,externals,&resident): resident externals bind their on-device buffer and are NOT re-uploaded; only activations are written per call. run/run_resident share one run_prepared core. A resident view binds exactly like a read-only storage input (slab_for routes it to weight_slab) and survives clear_transient_allocations. MEASURED (A2000, decode block d=576/kv=128/ffn=1536, FFN Wg/Wu/Wd=10.6MB resident): resident 9.605 ms/call vs all-upload 11.977 ms/call (~1.25x), 10.6MB/call no longer re-uploaded; run_resident == run BIT-FOR-BIT + matches CPU oracle across 3 calls (weights persist across the ring reset, zero numerical change). Win scales with weight size (~40MB/layer real → ~4x saved upload). 6/6 executor GPU certs pass; non-GPU wgsl_forge:: 134 pass/0 fail/46 ignored; lib plain + --features cuda EXIT 0. Note (Timothy: "no lanes, you've got the codebase"): the gguf_bridge/inference_agent decode-loop seam is now in scope for the Phase 1c p64->forge bridge + real-layer bake-off (next). Releasing claim.
2026-06-29 · Claude/feature-p64-manifold-wal-eigensolver · RELEASE · wgsl_forge/graph_ops/stencil.rs + wgsl_forge/emit/{hlsl.rs,msl.rs} + LLM_ON_FORGE_PROGRESS_LOG.md · Real RoPE — placeholder removed (directed by Timothy: "no placeholders"). RopePair was a FIXED-angle cos(1)/sin(1) "content-free structural rotation" — not RoPE. Now a real rotary position embedding: RopeConfig{head_dim,pos,mode,theta_base} + RopeMode::{Interleaved(GGUF NORM, pairs 2j/2j+1), Neox(GGUF NEOX/HF rotate_half, pairs j/j+hd/2)}; each pair rotated by true θ=pos·base^(-2j/head_dim) (default base 10000). rope_wgsl/rope_cpu/rope_gpu + RopeConfig; stencil_{cpu,gpu} now take RopeConfig and route RopePair through real RoPE (Laplacian unchanged). VERIFIED A2000: GPU cert (both conventions, head_dim 64, pos 5) matches CPU oracle; 7 non-GPU tests incl RELATIVE-POSITION INVARIANCE (RoPE's defining property: dot(RoPE(q,m),RoPE(k,n)) depends only on m-n, both modes) + pos-0-identity + per-pair-norm + naga-validate; full wgsl_forge:: 138 pass/0 fail/46 ignored; lib plain + --features cuda EXIT 0. ALSO scanned other placeholders: hlsl/msl "Simplified for now" vectorized-affine is CORRECT (affine-f32 is its only consumer, sole op in*scale+bias; native SIMD = unvalidatable-here optimization) — fixed the misleading comments only; fluid_dynamics/Divergence/Advection/quantum_bio remain honest Errs = curation item #45 (Timothy's physical-model call); unified zero-copy memory is an honest unverifiable-on-discrete-A2000 scope note. Releasing claim.
2026-06-29 · Claude/feature-p64-manifold-wal-eigensolver · RELEASE · wgsl_forge/ir/{graph.rs,q42_bridge.rs} + wgsl_forge/graph_ops/{slice.rs(NEW),mod.rs,executor.rs} + LLM_ON_FORGE_PROGRESS_LOG.md · Real multi-head (GQA) decode layer on the forge, GPU-certified on A2000 (directed: "a fake build loses; no placeholders"). Two real new IR ops: Slice{offset,len} (graph_ops/slice.rs — on-device contiguous sub-range, for per-head q/K/V slicing of node outputs) + Rope{head_dim,pos,mode,base_bits} (first-class RoPE reusing stencil::rope_wgsl/rope_cpu; pos in the params BUFFER not the source, so the pipeline cache stays warm across tokens). Opcodes 0x1B/0x1C + lower_graph/Lowerer arms + q42 encode/decode + executor GPU+CPU-oracle arms; every_op_class_roundtrips extended. decode_layer_graph(n_heads,n_kv_heads,head_dim,seq,ffn,pos,mode,base): RMSNorm->Q-proj->RoPE(q)->per-head GQA{slice q_h/Kt_h/V_h; scaled softmax(q_h·Kt_h)·V_h; o_h·Wo_h} summed->+x->RMSNorm->SwiGLU->+. Real grouped-query attention; per-head output proj summed (no concat op); head-major cache => contiguous slices. VERIFIED A2000: decode_layer_cpu_oracle_matches_reference (composed oracle vs INDEPENDENT hand-written reference, BOTH RoPE conventions, GQA 2:1) + decode_layer_gpu_matches_cpu_oracle (4 heads/2 kv-heads/head_dim 16, both conventions) PASS; non-GPU wgsl_forge:: 141 pass/0 fail/47 ignored; lib plain + --features cuda EXIT 0. Honest boundary: K/V from head-major cache externals (already RoPE'd, as the engine stores them); current-token K/V projection+append is the decode-loop seam (mutable cache state, not this functional graph); still f32, not yet p64-weights/inference_agent. Next: p64->forge bridge + one-real-layer bake-off vs hand-written dispatch_attention_pass/dispatch_ffn_pass. Releasing claim.
2026-06-29 · Claude/feature-p64-manifold-wal-eigensolver · RELEASE · wgsl_forge/graph_ops/executor.rs + LLM_ON_FORGE_PROGRESS_LOG.md · Bake-off prep (directed: "a fake build loses; continue"). Mapped the hand-written engine (the bake-off oracle) via 3 Explore scouts; closed two faithfulness gaps it revealed, both A2000-certified. (1) RMSNorm learned weight (commit acc6ed66): decode_layer_graph now scales each RMSNorm by attn_norm/ffn_norm (externals 8/9), matching the engine (x·inv_rms·w). (2) MatMul.trans_b was a SILENTLY-DROPPED flag — IR carried trans_b, executor `MatMul{m,n,k,tc,..}` dropped it + CPU oracle ignored it (latent fake). Now real: gemm_trans_b WGSL kernel + CPU oracle branch compute C[m,n]=A·Bᵀ with B bound [n,k] row-major = the native [out,in] GGUF/p64 weight layout, so the forge consumes p64 projection weights with NO transpose copy. Certs: trans_b CPU vs independent A·Bᵀ ref; A2000 GPU vs plain GEMM on explicitly-transposed B (different kernel path, gold cross-check). KEY ENGINE FACTS for the bridge: RoPE interleaved/base=rope_freq_base(100k SmolLM2)/scaled_pos=pos/rope_scale, K RoPE'd not V; weights [out,in] data layout; P64TensorIndex::from_p64 + .entries(role_id,dtype,manifold_idx,dimensions) + blob() + ggml_quants::dequantize_row_into; roles 0-11; NO model on disk (gitignored). VERIFIED: 8/8 executor GPU certs; non-GPU wgsl_forge:: 142 pass/0 fail/48 ignored; lib plain + --features cuda EXIT 0. ⚑ Timothy: drop a SmolLM2-360M GGUF in docs/models/ to unlock the real engine head-to-head + tok/s. Next: p64->forge bridge (trans_b native layout) + hermetic synthetic-p64 cert + model-gated bake-off. Releasing claim.
2026-06-29 · Claude/feature-p64-manifold-wal-eigensolver · RELEASE · wgsl_forge/graph_ops/p64_bridge.rs(NEW) + wgsl_forge/graph_ops/mod.rs + LLM_ON_FORGE_PROGRESS_LOG.md · p64 -> forge bridge: forge decode layer runs on REAL SmolLM2-360M weights (Timothy supplied C:\LLM_Models\GGUF\smollm2-360m-instruct-q8_0.gguf). p64_bridge.rs: read_role (locate role-tagged tensor in P64TensorIndex + dequant via ggml_quants::dequantize_row_into, handles Q8_0) + read_forge_layer_weights (Wq/Wo/Wg/Wu/Wd/attn_norm/ffn_norm, transposing 2-D projections from native [out,in] to the [in,out] the forge consumes). VERIFIED A2000: forge_decode_layer_on_real_p64_weights_matches_oracle loads the 386MB GGUF -> compile_gguf_to_p64 -> from_p64 -> bridge -> forge decode_layer_graph at REAL dims (n_embd 960, 15 heads, 5 kv-heads GQA, head_dim 64, ffn 2560, rope base 100k) == composed CPU oracle at MAX REL 3.28e-6. Skips if no model on disk. Non-GPU wgsl_forge:: 142 pass/0 fail/49 ignored; lib clean. Honest: weights/dims/rope real; x+KV-cache synthetic (certifies layer compute on real weights). Next: engine head-to-head (forge vs hand-written dispatch_transformer_layer on same weights, parity + ms/layer + tok/s) via to_gguf_index() + decode-loop seam ~inference_agent.rs:1077. Releasing claim.
2026-06-30 · Claude/feature-p64-manifold-wal-eigensolver · CLAIM · docs/manuals/standards/webizen-renderer-sdk-spec.md(NEW) + standards index.html + standards-backlog.md · Authoring a DRAFT renderer SDK spec (directed by Timothy: the N-D renderer is intended as an SDK employable in WASM or webizen-browser; a spec is the contract you build toward, not a writeup of finished code). Code-anchored to crates/webizen-render + qualia_core_db::render (Tensor10D/projection/pga/contract). Docs-only, standards/ folder — no code lane touched. Will RELEASE when written + surfaced + honest status table + to-do items captured.
2026-06-30 · Claude/feature-p64-manifold-wal-eigensolver · RELEASE · docs/manuals/standards/webizen-renderer-sdk-spec.md(NEW) + standards/index.html + standards-backlog.md · DRAFT renderer SDK spec written (directed: the N-D renderer is intended as an SDK in WASM or webizen-browser; spec is the contract you build toward, not a writeup of finished code). v0.1, code-anchored to crates/webizen-render + qualia_core_db::render. Covers: manifold→projection→view (project/manifold_world, ProjectionTarget Plane2D/Volume3D, pga semantic-motor oracle = GPU parity requirement); zero-heap GPU ABI (Motor 64B/RenderQuin 64B/Tensor10D 40B/CameraUniform+ObserverStandpoint 128B etc, all test-locked); neutral serde scene contract (RenderScene/SceneNode/Tensor10DProjection/EpistemicState); WgpuRenderer (offscreen→RGBA8/PNG/data-URI, scene draw, picking, orbit Camera); semantic layer (standpoint-gated visibility VAULT/DID, σ vision+audio parity+determinism, deontic lanes, VRAM ledger Full/Eco/Reserve); conformance (parity oracle, WGSL↔Rust binding coverage, ABI sizes, σ determinism, linear-passthrough offscreen). Every capability marked ✅implemented/◑partial/○draft-only. HONEST headline nuance documented: cross-platform webizen-render scene draw is presently 2D screen-space primitives (depth_stencil None) with a 3D camera available; the depth-buffered volumetric pipeline (depth/bloom/tensor-SOA/projector.wgsl/mesh) exists but is wasm-`portal`-gated (render::gpu::PortalGpu), not yet wired cross-platform — that's to-do §11.1/backlog item 13 (the headline gap). Captured 7 to-do items per Timothy's enhancements→to-do rule (volumetric wiring, unify CIE on engine spectral path, deontic cull stage, workspace membership+build verify, Phase 0.2b standalone qualia-render crate, SDK packaging, ⚑legacy webizen-browser decommission = his call). Surfaced in standards index under new "Renderer/viewport" group. Docs-only, standards/ — no code lane touched. Releasing claim.
2026-07-01 � Devin/feature-cfd-solver � CLAIM � crates/qualia-core-db/src/specialized_libs/engineering_analysis/cfd.rs(NEW) + engineering_analysis/mod.rs � Implementing real 2D incompressible Navier-Stokes finite-volume solver (projection method, Chorin). Building on the thermal_conduction.rs pattern as a submodule. Scope: structured Cartesian mesh, SIMPLE/projection pressure-velocity coupling, no-slip/inflow/outflow/pressure BCs, lid-driven cavity + channel flow tests. Releasing claim when tests pass.
