feat(portal): PR-C11b phenomenal viewport CI contract checklist

Add portal_phenomenal_contract.rs with WGSL parse smoke, bind-group layout
parity, uniform/stride size oracles, PGA R_q identity gate, and VramLedger
budget assertions.

Add docs/tests/phenomenal-verify.mjs orchestrator: wasm32 shader smoke,
contract tests, ambient draw step-down, and WASM navigation API exports.

Wire checklist into pages.yml after portal WASM build. Fix WGSL syntax caught
by smoke tests (f32 casts, module-level T_SCALE).