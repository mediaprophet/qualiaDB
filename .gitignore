/target
/crates/qualia-core-db/target
/crates/qualia-cli/target
/crates/webizen-desktop/target
/crates/webizen-studio/dist
*.rdf
!bundled/ontologies/fibo/rdf/**/*.rdf
*.wal
*.q42
*.n3
!bundled/qapps/**/*.n3

# Princeton WordNet — full graph is a GitHub Release asset (see docs/data/wordnet/README.md)
docs/data/wordnet/*.q42
docs/playground/wordnet.q42

# Ontology demo — Schema.org volume served from docs/data (CI + local)
!docs/data/schemaorg/**/*.q42

# Ontology demo — W3C volumes under docs/data/w3c (CI + local)
!docs/data/w3c/**/*.q42
!docs/data/w3c/**/*.q42.lex
!docs/data/w3c/**/*.q42.bidx

# Ontology demo — W3C archives (deduped ns.w3.org mirror)
!docs/data/w3c-archives/**/*.q42
!docs/data/w3c-archives/**/*.q42.lex
!docs/data/w3c-archives/**/*.q42.bidx
!bundled/ontologies/w3c-archives/**

# Ontology demo — PURL.org volumes under docs/data/purl
!docs/data/purl/**/*.q42
!docs/data/purl/**/*.q42.lex
!docs/data/purl/**/*.q42.bidx

# Ontology demo — GeoNames volume under docs/data/geonames
!docs/data/geonames/**/*.q42
!docs/data/geonames/**/*.q42.lex
!docs/data/geonames/**/*.q42.bidx

# Ontology demo — Dublin Core volume under docs/data/dublincore
!docs/data/dublincore/**/*.q42
!docs/data/dublincore/**/*.q42.lex
!docs/data/dublincore/**/*.q42.bidx

# Ontology demo — FIBO domain volumes (sources under bundled/ontologies/fibo/rdf)
!docs/data/fibo/**/*.q42
!docs/data/fibo/**/*.q42.lex
!docs/data/fibo/**/*.q42.bidx
docs/data/fibo/.build/
docs/data/fibo/**/*.nt

# Claude Code session files (dev-machine only)
.claude/

# Python bytecode cache
__pycache__/
*.pyc
*.pyo
*.pyd
*.py,cover

# Python virtualenvs, test caches, and packaging outputs
.python-version
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.tox/
.nox/
htmlcov/
site/
build/
dist/
*.egg-info/
pip-wheel-metadata/

# WordNet build artefacts (large downloaded data, not source)
scripts/.wordnet_build/

# Massive LLM Weights & Models
*.gguf
gemma-4-E4B-it-GGUF/

# wasm-pack output (copied to docs/playground/ manually or by pages.yml)
crates/qualia-core-db/pkg/

# Generated Rust API docs staged for Jekyll (CI only)
docs/rustdoc/

# Vendored Node.js binary (local tooling only, not needed in repo)
node_dist/
node.zip

# Local build outputs (Flutter packaging moved to WebizenAI repo)
/dist/
/releases/

# Local Libraries (export staging — see Local_LIbraries/EXPORTS/README.md)
Local_Libraries/

# App development sandbox (wellfair, cooperative-projects — not part of the core release)
qapp-development/
app-development/

# Signing keystore — never commit private keys
keystore.bin

# One-off migration scripts, old release binaries, debug artefacts
scratch/

# Build/debug error logs (PowerShell redirection output — UTF-16, not source)
*_errors.json
*_errors.txt
compile_errors.txt
cargo_err.txt
clippy.json
clippy_report.md

# QPU internal functionality (undocumented, private)

# Private research documents (not for public repository)
private/

# Local development files (completed planning, temporary docs, etc.)
local/
legacy/
ingest_raw.wal.tmp
yago-tests/ 
reports/
