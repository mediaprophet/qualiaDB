{
  "name": "qualia-diagnostics",
  "private": true,
  "version": "0.1.0",
  "description": "Bounded GGUF and browser/WebGPU diagnostic tools for QualiaDB",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "browser-smoke": "node browser_smoke.mjs"
  },
  "devDependencies": {
    "playwright": "1.62.0"
  }
}
